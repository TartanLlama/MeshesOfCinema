<h1>Listing mini_articles</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @mini_articles.each do |mini_article| %>
  <tr>
    <td><%= mini_article.name %></td>
    <td><%= link_to 'Show', mini_article %></td>
    <td><%= link_to 'Edit', edit_mini_article_path(mini_article) %></td>
    <td><%= link_to 'Destroy', mini_article, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />
