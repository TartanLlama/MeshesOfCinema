= form_for @movement do |f|
  -if @movement.errors.any?
    #error_explanation
      %h2= "#{pluralize(@movement.errors.count, "error")} prohibited this movement from being saved:"
      %ul
        - @movement.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
